# Pin GitHub Actions baseline support

This branch preserves the pre-script `pin-github-actions` skill and the fixture
files used by the experiment on `main`.

Run the comparison from the main branch. The experiment there loads the baseline
skill from this worktree at:

```text
.worktrees/experiments-baseline/plugins/security/skills/pin-github-actions
```
